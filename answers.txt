select id, name from topic order by name limit 10;

select tp.name from topic tp where (
	select count(p.id) from post p where p.topic_id = tp.id
) >= 5

select tp.name from topic tp where (
	select count(p.id) from post p where p.topic_id = tp.id
) = 0

OR

select tp.name from topic tp where not exists(
	select p.id from post p where p.topic_id = tp.id
)